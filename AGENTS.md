# 项目规则

## 本地服务
- 不要自行运行前端或后端服务。
- 可以打开浏览器验证页面。
- 如果验证时发现前端或后端没有启动，告知用户手动启动。

## 开发检查
- 开发阶段不需要打包。
- 检查语法、类型或明显代码错误时，优先使用 lint、类型检查或针对性测试。
- 只有用户明确要求打包、发布或验证生产构建时，才运行打包命令。

## 规则文件
- 以后用户说“写入规则文件”或“写入 AGENTS.md”时，默认写入项目级 `AGENTS.md`。
